type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Immediate Trade",
  site_description: "For educational purposes only. Not investment advice.",
  site_domain: "https://immediate.trade",
};
